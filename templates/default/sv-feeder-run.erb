#!/bin/sh

export NODE_PATH="<%= node['feeder']['home'] %>"
export NODE_PORT="<%= node['feeder']['mgmt_port'] %>"
export NODE_ENV="<%= node['feeder']['nodejs']['node_env'] %>"

cd $NODE_PATH
exec 2>&1

CACHEDIR=/srv/www/feeder/current exec chpst -u root /usr/local/bin/node $NODE_PATH/<%= node['feeder']['route'] %>.js
